{erl_opts, [debug_info]}.
{deps, []}.

{profiles, [
    {test, [
        {erl_opts, [
            debug_info,
            export_all,
            nowarn_export_all
        ]}
    ]}
]}.

{cover_enabled, true}.

{plugins, [rebar3_lint]}.
